import type { DecimalTuple } from "../types/DecimalTuple.js";
/**
 * Takes a `divisor` and `dividend` and returns the division of those values.
 *
 * @example
 * ```typescript
 * const half = decimalTupleDivide([2, 0]);
 *
 * half([5, 0]); // [25, -1] -> 2.5
 * ```
 * @param divisor Divisor `DecimalTuple` of the division.
 * @returns Curried function with `divisor` set in context.
 */
export declare const decimalTupleDivide: ([
	divisorCoefficient,
	divisorExponent,
]: DecimalTuple) => ([
	dividendCoefficient,
	dividendExponent,
]: DecimalTuple) => DecimalTuple;
