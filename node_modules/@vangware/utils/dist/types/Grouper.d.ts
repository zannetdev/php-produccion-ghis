/**
 * Grouper function.
 *
 * @template Item Type of the items in the source array.
 * @param item Item of the source array.
 * @returns Group name.
 */
export declare type Grouper<Item> = (item: Item) => number | string;
